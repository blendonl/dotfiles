# Auto-generated custom prefix table with which-key support

bind-key -T custom-prefix Space next-layout
bind-key -T custom-prefix ! break-pane
bind-key -T custom-prefix \" split-window
bind-key -r -T custom-prefix \# run-shell "/home/notpc/.config/tmux/scripts/window/create-window.sh DOCKER 'sudo docker compose up'"
bind-key -r -T custom-prefix \$ run-shell "/home/notpc/.config/tmux/scripts/window/create-window.sh REST nvim '/home/notpc/rest/collection/'"
bind-key -T custom-prefix \% split-window -h
bind-key -r -T custom-prefix & run-shell "/home/notpc/.config/tmux/scripts/window/create-window.sh GH gh"
bind-key -T custom-prefix \' command-prompt -T window-target -p index { select-window -t ":%%" }
bind-key -T custom-prefix ( switch-client -p
bind-key -T custom-prefix ) switch-client -n
bind-key -r -T custom-prefix * run-shell "/home/notpc/.config/tmux/scripts/window/create-window.sh NOTE gh"
bind-key -T custom-prefix , command-prompt -I "#W" { rename-window "%%" }
bind-key -r -T custom-prefix - break-pane
bind-key -T custom-prefix . command-prompt -T target { move-window -t "%%" }
bind-key -T custom-prefix / command-prompt -k -p key { list-keys -1N "%%" }
bind-key -T custom-prefix 0 select-window -t :=0
bind-key -T custom-prefix 1 select-window -t :=1
bind-key -T custom-prefix 2 select-window -t :=2
bind-key -T custom-prefix 3 select-window -t :=3
bind-key -T custom-prefix 4 select-window -t :=4
bind-key -T custom-prefix 5 select-window -t :=5
bind-key -T custom-prefix 6 select-window -t :=6
bind-key -T custom-prefix 7 select-window -t :=7
bind-key -T custom-prefix 8 select-window -t :=8
bind-key -T custom-prefix 9 select-window -t :=9
bind-key -T custom-prefix : command-prompt
bind-key -T custom-prefix \; last-pane
bind-key -T custom-prefix < display-menu -T "#[align=centre]#{window_index}:#{window_name}" -x W -y W "#{?#{>:#{session_windows},1},,-}Swap Left" l { swap-window -t :-1 } "#{?#{>:#{session_windows},1},,-}Swap Right" r { swap-window -t :+1 } "#{?pane_marked_set,,-}Swap Marked" s { swap-window } '' Kill X { kill-window } Respawn R { respawn-window -k } "#{?pane_marked,Unmark,Mark}" m { select-pane -m } Rename n { command-prompt -F -I "#W" { rename-window -t "#{window_id}" "%%" } } '' "New After" w { new-window -a } "New At End" W { new-window }
bind-key -r -T custom-prefix = join-pane -h -t 2
bind-key -T custom-prefix > display-menu -T "#[align=centre]#{pane_index} (#{pane_id})" -x P -y P "#{?#{m/r:(copy|view)-mode,#{pane_mode}},Go To Top,}" < { send-keys -X history-top } "#{?#{m/r:(copy|view)-mode,#{pane_mode}},Go To Bottom,}" > { send-keys -X history-bottom } '' "#{?mouse_word,Search For #[underscore]#{=/9/...:mouse_word},}" C-r { if-shell -F "#{?#{m/r:(copy|view)-mode,#{pane_mode}},0,1}" "copy-mode -t=" ; send-keys -X -t = search-backward -- "#{q:mouse_word}" } "#{?mouse_word,Type #[underscore]#{=/9/...:mouse_word},}" C-y { copy-mode -q ; send-keys -l "#{q:mouse_word}" } "#{?mouse_word,Copy #[underscore]#{=/9/...:mouse_word},}" c { copy-mode -q ; set-buffer "#{q:mouse_word}" } "#{?mouse_line,Copy Line,}" l { copy-mode -q ; set-buffer "#{q:mouse_line}" } '' "#{?mouse_hyperlink,Type #[underscore]#{=/9/...:mouse_hyperlink},}" C-h { copy-mode -q ; send-keys -l "#{q:mouse_hyperlink}" } "#{?mouse_hyperlink,Copy #[underscore]#{=/9/...:mouse_hyperlink},}" h { copy-mode -q ; set-buffer "#{q:mouse_hyperlink}" } '' "Horizontal Split" h { split-window -h } "Vertical Split" v { split-window -v } '' "#{?#{>:#{window_panes},1},,-}Swap Up" u { swap-pane -U } "#{?#{>:#{window_panes},1},,-}Swap Down" d { swap-pane -D } "#{?pane_marked_set,,-}Swap Marked" s { swap-pane } '' Kill X { kill-pane } Respawn R { respawn-pane -k } "#{?pane_marked,Unmark,Mark}" m { select-pane -m } "#{?#{>:#{window_panes},1},,-}#{?window_zoomed_flag,Unzoom,Zoom}" z { resize-pane -Z }
bind-key -T custom-prefix ? list-keys -N
bind-key -r -T custom-prefix @ run-shell "/home/notpc/.config/tmux/scripts/window/create-window.sh RUN"
bind-key -r -T custom-prefix A run-shell "/home/notpc/.config/tmux/scripts/window/create-window.sh AI-CONTINUE 'claude --continue'"
bind-key -T custom-prefix C customize-mode -Z
bind-key -T custom-prefix D choose-client -Z
bind-key -T custom-prefix E select-layout -E
bind-key -r -T custom-prefix H run-shell "/home/notpc/.config/tmux/scripts/tmux-sessions.sh /mnt/data/personal/fppk/tt-score-api/"
bind-key -T custom-prefix I run-shell /home/notpc/.config/tmux/plugins/tpm/bindings/install_plugins
bind-key -r -T custom-prefix L run-shell "/home/notpc/.config/tmux/scripts/tmux-sessions.sh /mnt/data/personal/fppk/tt-score-dashboard/"
bind-key -T custom-prefix M select-pane -M
bind-key -r -T custom-prefix Q set-option -g status on
bind-key -r -T custom-prefix S set-option -g status off
bind-key -T custom-prefix U run-shell /home/notpc/.config/tmux/plugins/tpm/bindings/update_plugins
bind-key -T custom-prefix [ copy-mode
bind-key -T custom-prefix ] paste-buffer -p
bind-key -r -T custom-prefix ` send-keys `
bind-key -T custom-prefix a switch-client -T ai-mode
bind-key -r -T custom-prefix b run-shell /home/notpc/.config/tmux/scripts/tmux-sessions.sh
bind-key -T custom-prefix c new-window
bind-key -T custom-prefix d detach-client
bind-key -T custom-prefix f command-prompt { find-window -Z "%%" }
bind-key -T custom-prefix i display-message
bind-key -T custom-prefix k run-shell -b "/home/notpc/.config/tmux/scripts/modes/show-which-key.sh mkanban-mode" \; switch-client -T mkanban-mode
bind-key -T custom-prefix l last-window
bind-key -T custom-prefix m select-pane -m
bind-key -T custom-prefix n next-window
bind-key -T custom-prefix o select-pane -t :.+
bind-key -T custom-prefix p previous-window
bind-key -T custom-prefix q display-panes
bind-key -r -T custom-prefix r run-shell "/home/notpc/.config/tmux/scripts/modes/source_modes.sh && tmux source-file ~/.config/tmux/tmux.conf && tmux display-message 'Tmux config reloaded!'"
bind-key -T custom-prefix s choose-tree -Zs
bind-key -T custom-prefix t clock-mode
bind-key -T custom-prefix v split-window -l "30%"
bind-key -T custom-prefix w choose-tree -Zw
bind-key -T custom-prefix x confirm-before -p "kill-pane #P? (y/n)" kill-pane
bind-key -T custom-prefix z resize-pane -Z
bind-key -T custom-prefix \{ swap-pane -U
bind-key -T custom-prefix | split-window -h -l "30%"
bind-key -T custom-prefix \} swap-pane -D
bind-key -T custom-prefix \~ show-messages
bind-key -r -T custom-prefix DC refresh-client -c
bind-key -T custom-prefix PPage copy-mode -u
bind-key -r -T custom-prefix Up select-pane -U
bind-key -r -T custom-prefix Down select-pane -D
bind-key -r -T custom-prefix Left select-pane -L
bind-key -r -T custom-prefix Right select-pane -R
bind-key -T custom-prefix M-1 select-layout even-horizontal
bind-key -T custom-prefix M-2 select-layout even-vertical
bind-key -T custom-prefix M-3 select-layout main-horizontal
bind-key -T custom-prefix M-4 select-layout main-vertical
bind-key -T custom-prefix M-5 select-layout tiled
bind-key -T custom-prefix M-6 select-layout main-horizontal-mirrored
bind-key -T custom-prefix M-7 select-layout main-vertical-mirrored
bind-key -T custom-prefix M-n next-window -a
bind-key -T custom-prefix M-o rotate-window -D
bind-key -T custom-prefix M-p previous-window -a
bind-key -T custom-prefix M-u run-shell /home/notpc/.config/tmux/plugins/tpm/bindings/clean_plugins
bind-key -r -T custom-prefix M-Up resize-pane -U 5
bind-key -r -T custom-prefix M-Down resize-pane -D 5
bind-key -r -T custom-prefix M-Left resize-pane -L 5
bind-key -r -T custom-prefix M-Right resize-pane -R 5
bind-key -r -T custom-prefix C-A run-shell "/home/notpc/.config/tmux/scripts/window/join-window.sh AI-CONTINUE 'claude --continue'"
bind-key -T custom-prefix C-a send-prefix
bind-key -r -T custom-prefix C-e run-shell "/home/notpc/.config/tmux/scripts/window/join-window.sh DOCKER 'sudo docker compose up'"
bind-key -r -T custom-prefix C-i run-shell "/home/notpc/.config/tmux/scripts/window/join-window.sh NOTE gh"
bind-key -T custom-prefix C-o rotate-window
bind-key -r -T custom-prefix C-r run-shell "/home/notpc/.config/tmux/scripts/window/join-window.sh REST nvim '/home/notpc/rest/collection/'"
bind-key -r -T custom-prefix C-u run-shell "/home/notpc/.config/tmux/scripts/window/join-window.sh GH gh"
bind-key -r -T custom-prefix C-w run-shell "/home/notpc/.config/tmux/scripts/window/join-window.sh RUN"
bind-key -T custom-prefix C-z suspend-client
bind-key -r -T custom-prefix C-Up resize-pane -U 10
bind-key -r -T custom-prefix C-Down resize-pane -D 10
bind-key -r -T custom-prefix C-Left resize-pane -L 10
bind-key -r -T custom-prefix C-Right resize-pane -R
bind-key -r -T custom-prefix S-Up refresh-client -U 10
bind-key -r -T custom-prefix S-Down refresh-client -D 10
bind-key -r -T custom-prefix S-Left refresh-client -L 10
bind-key -r -T custom-prefix S-Right refresh-client -R 10

